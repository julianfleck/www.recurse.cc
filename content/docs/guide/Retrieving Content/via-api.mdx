---
title: via API
---

# Search

- tree is the standard object
    - use `depth` parameter to control tree expansion
    - expanded tree includes parent nodes with summaries, makes responses more accurate
    - if you need flat nodes, use `depth=0`